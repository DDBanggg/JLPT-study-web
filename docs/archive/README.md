# Documentation archive

Files under this directory are historical records retained for project context. They are
not current sources of truth and must not override the canonical specifications in
[`docs/specs`](../specs), the operational guidance in [`docs/guides`](../guides), or the
active roadmaps in [`docs/progress`](../progress).
